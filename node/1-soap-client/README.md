# Cliente SOAP en Node.js

Este subproyecto consume el servicio SOAP público `NumberConversion` para convertir números a palabras en inglés.

## Cómo ejecutarlo

1. Instalar dependencias: `npm install`
2. Levantar el servidor: `node index.js`
3. Probar en el navegador: `http://localhost:3001/?numero=123`
